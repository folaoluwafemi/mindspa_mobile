class UiHelper {
  /// This class Serves are the UI helper, it will provide consistent styling of the UI
  /// Its also has a private constructor so that the class cannot be instantiated outside the class.
  /// You can only access the static fields.
  UiHelper._();
  //TODO: Convert the fields to a SizedBox widget with vertical and horizontal versions e.g kVerticalSmallSpace.s
  //TODO: Implement the padding and margin fields e.g "KlargePadding"

  static const double zero = 0;
  static const double tiny = 4;
  static const double small = 8;
  static const double medium = 16;
  static const double big = 24;
  static const double large = 32;
}
